<Window x:Class="Sandy.Agent.Views.EnrollmentWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Set up Sandy" Width="460" Height="520" ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen" Background="#151923" Foreground="White">
  <Grid Margin="32">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>
    <TextBlock FontSize="28" FontWeight="SemiBold" Text="Connect this PC" />
    <TextBlock Grid.Row="1" Margin="0,10,0,22" TextWrapping="Wrap"
               Foreground="#B8BFCC" Text="Enter the Sandy server address and the family join code shown to a parent." />
    <StackPanel Grid.Row="2">
      <TextBlock Text="Server address" />
      <TextBox x:Name="ServerUrl" Margin="0,6,0,16" Padding="10" Text="https://sandy.example.com" />
    </StackPanel>
    <StackPanel Grid.Row="3">
      <TextBlock Text="Family join code" />
      <TextBox x:Name="JoinCode" Margin="0,6,0,16" Padding="10" CharacterCasing="Upper" />
    </StackPanel>
    <StackPanel Grid.Row="4">
      <TextBlock Text="Device name" />
      <TextBox x:Name="DeviceName" Margin="0,6,0,20" Padding="10" />
    </StackPanel>
    <StackPanel Grid.Row="5">
      <Button x:Name="ConnectButton" Height="44" Content="Connect" IsDefault="True"
              Background="{StaticResource AccentBrush}" Foreground="#0B2117"
              BorderThickness="0" FontSize="16" FontWeight="SemiBold"
              VerticalContentAlignment="Center" Click="Connect_Click" />
      <TextBlock x:Name="ErrorText" Margin="0,14,0,0" Foreground="#FF8A80" TextWrapping="Wrap" />
    </StackPanel>
  </Grid>
</Window>
