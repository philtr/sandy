<Window x:Class="Sandy.Agent.Views.WarningWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Width="440" Height="210" WindowStyle="None" Topmost="True" ShowInTaskbar="False"
        WindowStartupLocation="Manual" Background="#F21E2430">
  <Border BorderBrush="#FFFFB300" BorderThickness="3" Padding="24">
    <StackPanel>
      <TextBlock x:Name="MessageText" Foreground="White" FontSize="28" FontWeight="SemiBold"
                 TextAlignment="Center" TextWrapping="Wrap" />
      <Grid Margin="0,20,0,0" HorizontalAlignment="Center">
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="Auto" />
          <ColumnDefinition Width="16" />
          <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <Grid Width="44" Height="44" AutomationProperties.Name="Auto-dismiss countdown">
          <Ellipse Stroke="#40FFFFFF" StrokeThickness="4" />
          <Ellipse Stroke="#FFFFB300" StrokeThickness="4" StrokeDashArray="10 18"
                   StrokeStartLineCap="Round" StrokeEndLineCap="Round"
                   RenderTransformOrigin="0.5,0.5">
            <Ellipse.RenderTransform>
              <RotateTransform />
            </Ellipse.RenderTransform>
            <Ellipse.Triggers>
              <EventTrigger RoutedEvent="Loaded">
                <BeginStoryboard>
                  <Storyboard>
                    <DoubleAnimation
                        Storyboard.TargetProperty="(UIElement.RenderTransform).(RotateTransform.Angle)"
                        From="0" To="360" Duration="0:0:1" RepeatBehavior="Forever" />
                  </Storyboard>
                </BeginStoryboard>
              </EventTrigger>
            </Ellipse.Triggers>
          </Ellipse>
          <TextBlock x:Name="CountdownText" Text="10" Foreground="White" FontSize="15"
                     FontWeight="SemiBold" HorizontalAlignment="Center" VerticalAlignment="Center" />
        </Grid>

        <Button Grid.Column="2" Width="110" Height="36" Content="Got it"
                VerticalAlignment="Center" Click="Close_Click" />
      </Grid>
    </StackPanel>
  </Border>
</Window>
