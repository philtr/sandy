<Window x:Class="Sandy.Agent.Views.CountdownWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Width="440" Height="190" WindowStyle="None" AllowsTransparency="True"
        Background="Transparent" Topmost="True" ShowActivated="False" ShowInTaskbar="False"
        WindowStartupLocation="Manual" IsHitTestVisible="False" Focusable="False">
  <Border Margin="8" Background="{StaticResource SurfaceOpaqueBrush}" BorderBrush="#805A9FF5"
          BorderThickness="1" CornerRadius="20" Padding="22">
    <Border.Effect>
      <DropShadowEffect BlurRadius="22" ShadowDepth="6" Opacity="0.55" Color="#FF000000" />
    </Border.Effect>
    <Grid>
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="Auto" />
        <ColumnDefinition Width="*" />
      </Grid.ColumnDefinitions>
      <Border Width="48" Height="48" CornerRadius="15" Background="#2AF4B942"
              BorderBrush="{StaticResource WarningBrush}" BorderThickness="1" VerticalAlignment="Center">
        <TextBlock Text="!" Foreground="{StaticResource WarningBrush}" FontSize="27" FontWeight="Bold"
                   HorizontalAlignment="Center" VerticalAlignment="Center" />
      </Border>
      <StackPanel Grid.Column="1" Margin="18,0,0,0" VerticalAlignment="Center">
        <StackPanel Orientation="Horizontal">
          <TextBlock Text="Sandy" FontSize="14" FontWeight="SemiBold" Foreground="{StaticResource AccentBrush}" />
          <TextBlock Text=" · Screen time ends in" FontSize="14" Foreground="{StaticResource MutedBrush}" />
        </StackPanel>
        <TextBlock x:Name="RemainingText" Foreground="White" FontSize="60" LineHeight="63"
                   FontWeight="Bold" Text="1:00" />
      </StackPanel>
    </Grid>
  </Border>
</Window>
