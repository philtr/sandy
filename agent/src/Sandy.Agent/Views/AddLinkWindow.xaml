<Window x:Class="Sandy.Agent.Views.AddLinkWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Add a link" Width="520" Height="330" ResizeMode="NoResize" WindowStartupLocation="CenterOwner"
        Background="#0D131E" Foreground="White">
  <Grid Margin="28">
    <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>
    <TextBlock FontSize="28" FontWeight="SemiBold" Text="Add a launcher link" />
    <StackPanel Grid.Row="1" Margin="0,18,0,0">
      <TextBlock Text="Name" /><TextBox x:Name="NameText" Margin="0,6,0,0" Padding="10" />
    </StackPanel>
    <StackPanel Grid.Row="2" Margin="0,14,0,0">
      <TextBlock Text="URI" /><TextBox x:Name="UriText" Margin="0,6,0,0" Padding="10" />
      <TextBlock x:Name="ErrorText" Margin="0,6,0,0" Foreground="#FF8A80" TextWrapping="Wrap" />
    </StackPanel>
    <StackPanel Grid.Row="3" Margin="0,18,0,0" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Bottom">
      <Button Margin="0,0,10,0" Padding="18,9" Content="Cancel" Click="Cancel_Click" />
      <Button Padding="18,9" Content="Add" IsDefault="True" Click="Add_Click" />
    </StackPanel>
  </Grid>
</Window>
