<% content_for :title, "Family created" %>
<section class="panel narrow center auth-card">
  <div class="result-icon" aria-hidden="true">✓</div>
  <p class="eyebrow">Setup complete</p>
  <h1>Save the PC <span class="gradient-text">join code</span></h1>
  <p class="lede">The Windows agent asks for this code on first launch. It is shown only now.</p>
  <div class="join-code"><%= @join_code %></div>
  <%= link_to "Open dashboard", root_path, class: "primary-button" %>
</section>
