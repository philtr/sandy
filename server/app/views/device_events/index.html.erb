<% content_for :title, "Events · #{@device.name} · Sandy" %>
<%= link_to "← #{@device.name}", device_path(@device), class: "back-link" %>
<section class="panel history-page" aria-labelledby="history-title">
  <div class="history-page-heading">
    <div>
      <p class="eyebrow"><%= @device.name %></p>
      <h1 id="history-title">Events</h1>
    </div>
    <span class="panel-kicker"><%= pluralize(@total_count, "event") %></span>
  </div>
  <%= render "devices/events", events: @events %>
  <%= render "shared/pagination", page: @page, total_pages: @total_pages, label: "Events" %>
</section>
