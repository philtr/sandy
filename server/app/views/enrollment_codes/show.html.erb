<% content_for :title, "New PC join code" %>
<section class="panel narrow center">
  <p class="eyebrow">Join code rotated</p>
  <h1>Save the new PC join code</h1>
  <p>The previous code no longer works. Existing enrolled devices are unaffected.</p>
  <div class="join-code"><%= @join_code %></div>
  <%= link_to "Return to dashboard", root_path, class: "primary-button" %>
</section>
